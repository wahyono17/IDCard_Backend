IDCard backend with sql server database and codeigniter framework in controller/api/employee all proses CRUD is available 
if you using mssql plesae make sure you have install driver sqlsvr you can change database setting in application/config/database.php 
Employee_id,First_Name,Date_of_join, Date_of_expired, Dept_id, Code_id is the column in the employee table